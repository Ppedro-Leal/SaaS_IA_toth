/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: [
            "https://oaidalleapiprodscus.blob.core.windows.net",
        ]
    }
}

module.exports = nextConfig
